.vscode

# Agent state written by ai_sandbox.sh (devopscoop/scripts) when SANDBOX_HOME points into the project
# (the default is ~/.sandbox-home). Contains credentials -- never commit it.
.sandbox-home/

